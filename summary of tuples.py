# Methods in tuples:

# no append, no pop, no insert, no remove   # We can apply these methods in the list inside the tuple i.e. (12,[1,4,5])
# only count and index

# Functions in tuples:

# min(), max(), len(), sum()